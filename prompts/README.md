# Prompts

System and user prompt templates for the AI engine.

## Structure (planned)

```
prompts/
├── system/
├── chart_analysis/
├── compatibility/
└── daily_horoscope/
```

Prompts are loaded at runtime via `PROMPTS_PATH` (see `.env.example`).
